"""
Trinity UI Package (Canonical)

This `ui` package is owned by Trinity (Z+3) and provides the canonical UI layer
for LightSpeed (themes, Bento overlay, IT Portal, orchestrator, etc.).

Note: Other floors may contain a `ui/` directory for future floor-specific UI
assets/adapters, but they must not shadow this package. Avoid placing
`__init__.py` in non-Trinity `ui/` folders.
"""

__all__: list[str] = []
