# LS GO Sites side-edit integration

The existing LightSpeed GO site remains the only target. This change set does not create a replacement site.

The current Git source is `apps/lightspeed-go` on `main`. Drive remains the evidence and workbook control layer. The side edit should preserve the Command, Activity, System, and Sources views and add only current-site identity, owner orientation, source parity, and a publish receipt.
